{
  "description": "Bundler gems require immutable lowercase SHA-256 checksums",
  "operations": [
    {"op": "replace", "path": "/profiles/0/bundler_gem_sha256", "value": "not-a-sha256"}
  ],
  "expected": "does not match pattern"
}
