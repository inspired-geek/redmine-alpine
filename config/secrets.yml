<% secret_key_base = ENV.fetch("SECRET_KEY_BASE") { ENV.fetch("REDMINE_SECRET_KEY_BASE") } %>
<% raise ArgumentError, "SECRET_KEY_BASE must not be empty" if secret_key_base.empty? %>
production:
  secret_key_base: <%= secret_key_base.inspect %>
